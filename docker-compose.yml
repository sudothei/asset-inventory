version: "3"
services:
  web:
    env_file:
      - .env
    build:
      context: ./web
    ports:
      - "80:80"
      - "443:443"
  api:
    env_file:
      - .env
    build:
      context: ./api
      args:
        CARGO_REGISTRY: ${CARGO_REGISTRY}
    ports:
      - "80:80"
      - "443:443"
